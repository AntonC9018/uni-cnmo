#pragma once
#include <func/ui/basicplot.h>
#include <func/func.h>

namespace Poly
{
    class Plot: public BasicPlot
    {
        Q_OBJECT

    public:
        explicit Plot(QWidget * parent = NULL);

    private:
    };
}